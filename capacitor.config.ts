import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.crowd.bible',
  appName: 'crowd.Bible',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
